# Local development environment for Inputly
# Runs with docker-compose.dev.yml alongside Neon Local proxy

# App
PORT=5432
NODE_ENV=development
LOG_LEVEL=debug

# JWT
JWT_SECRET=dev_super_secret_change_me
JWT_EXPIRES_IN=7d

# Arcjet (optional for security middleware)
ARCJET_KEY=ajkey_01k5t2gst0ef4te87wvpaqyy2d

# Neon Local proxy configuration
# Required by docker-compose.dev.yml to create ephemeral branches
NEON_API_KEY= napi_8v6upp0j56fzx81f4r6flt2r9rtuldfs7ozkj0xul1n0gij3auo3blbofk98iyz8
NEON_PROJECT_ID=org-late-morning-57145960
# Optional: parent branch id; leave empty to default or set from Neon console
PARENT_BRANCH_ID=br-hidden-math-aguqoxsv


DELETE_BRANCH=true
# Database connection for app inside Docker Compose network
# Using Neon Local proxy service hostname `neon-local`
# Username and password are `neon:npg` as per Neon Local docs
DATABASE_URL=postgres://neon:npg@neon-local:5432/neondb?sslmode=require
